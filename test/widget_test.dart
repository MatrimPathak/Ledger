import 'package:flutter_test/flutter_test.dart';
import 'package:ledger/app.dart';

void main() {
  testWidgets('App class exists', (WidgetTester tester) async {
    expect(LedgerApp, isNotNull);
  });
}
