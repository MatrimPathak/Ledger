{
  "name": "ledger-functions",
  "description": "Cloud Functions for Ledger — currently just the balance reconciliation check.",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "test": "node --test test/reconcile.test.js"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.5.0"
  }
}
